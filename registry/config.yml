version: 0.1
log:
  fields:
    service: registry
storage:
  filesystem:
    rootdirectory: /var/lib/registry
  delete:
    enabled: true
http:
  addr: "0.0.0.0:5000"
  headers:
    X-Content-Type-Options: [nosniff]
auth:
  htpasswd:
    realm: Autello Registry
    path: /auth/htpasswd
